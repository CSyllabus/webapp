import { CsyllabusPage } from './app.po';

describe('csyllabus App', () => {
  let page: CsyllabusPage;

  beforeEach(() => {
    page = new CsyllabusPage();
  });

 /* it('should display welcome message', () => {
    page.navigateTo();
    expect(page.getParagraphText()).toEqual('Welcome to app!');
  });*/
});
